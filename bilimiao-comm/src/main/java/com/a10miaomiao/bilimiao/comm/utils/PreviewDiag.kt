package com.a10miaomiao.bilimiao.comm.utils


/**
 * 「拖动进度条预览图」临时诊断日志 —— 只为定位"拖动没有预览图"用。
 *
 * 预览图问题已在 2026-09-20 定位并修好 → 按发布前的清理要求**整体停用**：
 * 函数签名保留、实现清空，不再创建、不再写入任何文件。
 *
 * 为什么保留调用点（而不是把 14 处调用一起删掉）：线上再遇到同类问题，
 * 把这里的实现恢复即可，不用重新插一遍埋点；现在是空方法，R8 会把调用整段优化掉。
 */
object PreviewDiag {

    /** 记一行诊断日志（发布版：不落盘、不输出） */
    @Suppress("UNUSED_PARAMETER")
    fun log(msg: String) = Unit
}
